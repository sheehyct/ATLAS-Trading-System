# Session Startup Prompt - EQUITY-65

**Previous Session:** EQUITY-64 (January 15-16, 2026)
**Current Branch:** `main`
**VPS Status:** Running (1H bar alignment + os import fixes deployed)
**Plan Mode:** REQUIRED - Multiple filter issues causing no trade execution

---

## CRITICAL: No Trades Executing - Root Causes Found

Investigation on Jan 16 identified 6 root causes. **Plan mode required** to decide fixes.

| Priority | Cause | Quick Fix |
|----------|-------|-----------|
| **CRITICAL** | `SIGNAL_EXECUTION_ENABLED` defaults false | `export SIGNAL_EXECUTION_ENABLED=true` |
| **HIGH** | TFC Filter rejects TFC < 3 for 1H | `export SIGNAL_TFC_FILTER_ENABLED=false` |
| **MEDIUM** | TFC Re-eval threshold = 3 | `export SIGNAL_TFC_REEVAL_MIN_STRENGTH=2` |
| **MEDIUM** | 1H TFC checks 4 TFs, needs 3/4 | Code adjustment needed |
| **MEDIUM** | Stale setup filter rejects 3-? | Known EQUITY-62 issue |

---

## EQUITY-64 Accomplishments

1. **1H Bar Alignment Fix** (6eb9ef8): Market-open aligned bars (9:30, 10:30, 11:30)
2. **Missing os Import Fix** (1c664f8): Pre-existing bug causing resampled scan errors
3. **Alpaca Keys Updated**: New SMALL account keys deployed
4. **Pipeline Investigation**: Root causes documented in PIPELINE_DEBUG_FINDINGS.md

---

## EQUITY-65 Priorities

### Priority 1: Decide Filter Thresholds

TFC filters (EQUITY-62) may be too aggressive:
- 1H requires 3/4 TFC (75% alignment)
- TFC re-eval blocks entry if TFC degrades below 3

Options:
A. Disable TFC filter entirely (`SIGNAL_TFC_FILTER_ENABLED=false`)
B. Lower thresholds (1H: 2/4 instead of 3/4)
C. Keep current (accept fewer trades but higher quality)

### Priority 2: Fix Stale Setup Filter for 3-? Patterns

3-? patterns have entry AFTER setup bar closes. Current filter marks valid triggers as "stale".

Location: `daemon.py:871-962`

### Priority 3: Verify Execution Enabled

Check VPS environment: `echo $SIGNAL_EXECUTION_ENABLED`

---

## VPS Debug Commands

```bash
# Check environment
ssh atlas@178.156.223.251 "env | grep SIGNAL"

# Check recent rejections
ssh atlas@178.156.223.251 "sudo journalctl -u atlas-daemon --since today | grep -E 'FILTER REJECTED|STALE SETUP'"

# Run diagnostic
ssh atlas@178.156.223.251 "cd /home/atlas/vectorbt-workspace && python3 scripts/diagnose_pipeline_simple.py"
```

---

## Reference Documents

- HANDOFF: `docs/HANDOFF.md`
- Pipeline Debug: `PIPELINE_DEBUG_FINDINGS.md` (branch `claude/debug-pipeline-trade-execution-3pqLW`)
- CLAUDE.md: `docs/CLAUDE.md` (STRICT COMPLIANCE)

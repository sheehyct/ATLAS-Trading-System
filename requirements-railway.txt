# Railway deployment requirements (excludes VectorBT Pro private repo)
# Dashboard and API dependencies only

# Web framework
dash>=2.14.0
dash-bootstrap-components>=1.5.0
plotly>=6.2.0

# Data handling
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.16.0

# Trading APIs (Alpaca + Tiingo)
alpaca-py>=0.42.0
alpaca-trade-api>=3.2.0
requests>=2.32.4

# Async HTTP clients for dashboard performance
httpx>=0.27.0  # Primary async client with HTTP/2 support
aiohttp>=3.9.0  # Fallback async client

# Regime detection dependencies
scikit-learn>=1.3.0
hmmlearn>=0.3.0
holidays>=0.79
pandas-market-calendars>=4.0.0

# Configuration
python-dotenv>=1.1.1

# Logging/utilities
rich>=13.7.1

# Optional data source (if Tiingo unavailable)
yfinance>=0.2.40
